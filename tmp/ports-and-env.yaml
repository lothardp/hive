port_vars:
  - PORT
  - DB_PORT
  - REDIS_PORT

env:
  NODE_ENV: development
  LOG_LEVEL: debug

hooks:
  - echo "PORT=$PORT DB_PORT=$DB_PORT REDIS_PORT=$REDIS_PORT"
  - echo "NODE_ENV=$NODE_ENV HIVE_CELL=$HIVE_CELL"
